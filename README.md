# ipp

Reusable Terraform module for an SQS-triggered container Lambda function with DLQ, CloudWatch logs and alarms, and optional VPC attachment.

See `terraform/readme.md` for the full module surface, defaults, and production notes. Working examples live in `terraform/examples/minimal/` and `terraform/examples/with-vpc/`.
